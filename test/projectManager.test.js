'use strict';
var ProjectManager = require('../lib/projectManager');

describe('projectManager', function() {
  var projectManager;

  beforeEach(function() {
    projectManager = new ProjectManager();
  });

  it('should allow you to create a new project', function() { 
  });
});
